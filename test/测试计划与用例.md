# 定稿AI · 测试计划与用例（开发版 v0.4 · 本地优先架构）

> 角色：测试工程师 ｜ 覆盖：功能 / 性能 / 兼容性 / 合规 / 安全 ｜ 自动化脚本：`test/api.test.js`（97 条用例）

## 一、测试范围

| 维度 | 范围 |
|---|---|
| 功能 | 登录注册会话（30天）；写作全流程（本地优先）：选题助手（M9，纯中转AI六要素/追问）、大纲（M10，纯中转生成＋本地树编辑）、本地编写（M11，IndexedDB自动保存/浏览器端Word导出确定性）、进度打卡（M12，本地统计/增量/连续天数）；互译（M4）、交稿检查（M8）、引用核查/分段校对/四要素/综述（S7/S8/C7/C8 保留）；记录 CRUD；导入（.txt/.docx/.pdf）、导出（.txt/.docx） |
| 性能 | 长文本（10 万字符上限）处理、分段校对长文回归（7千+字全程完成）、单块超时降级 |
| 兼容性 | Chrome / Edge / Firefox / Safari；Windows / macOS；≥1366×768 与高分辨率屏；**IndexedDB 支持（Safari 隐私模式受限需提示）** |
| 合规 | 内容0修改红线、AI 输出标注「需人工核验」、**论文全文仅存用户浏览器本地（服务端零存储，V1.4 红线）**、密钥不泄露到前端、限流 |
| 安全 | 认证与越权隔离（论文本地分库天然隔离）、密码哈希（scrypt）、XSS 转义、请求大小与文本长度限制 |

## 二、用例清单（自动化 97 条）

### 用例组1-11 沿用 v0.2/v0.3（TC-01 ~ TC-69，口径升级 v0.4）
- 认证与安全（TC-01~08，TC-08 版本号验 v0.4 与 privacy 声明）
- 引用核查规则引擎（TC-09~12）、AI 流式（TC-13~18）、记录（TC-19~25）、导入导出（TC-26~31）、合规安全（TC-32~36）、BYOK（TC-37~45）、分段校对长文回归（TC-46~52）、引用核查增强（TC-53~58）、交稿检查（TC-59~64）、前端完整性（TC-65~69）

### 用例组12 M9 选题助手（纯中转·真实 DeepSeek，TC-70 ~ TC-74）
| 编号 | 用例 | 预期 |
|---|---|---|
| TC-70 | 选题建议生成 | 200，3-5 个选题 |
| TC-71 | 纯中转无会话落库 | 响应无 session_id，aiNote 含「不存储」 |
| TC-72 | 六要素齐全率 | ≥80% 选题六要素齐全（不足自动修正重试） |
| TC-73 | 追问迭代（前端携带历史） | 200，重新返回选题 |
| TC-74 | 空追问 | 400 拒绝 |

### 用例组13 M10 大纲 + 本地优先架构断言（TC-75 ~ TC-78）
| TC-75 | AI 生成大纲 | 200，≥5章 |
| TC-76 | **服务端已移除论文存储端点** | /api/thesis·chapters·outline(树)·progress·checkins·thesis/export 全部 404 |
| TC-77 | **测试库无论文数据表** | sqlite_master 中无 theses/chapters/checkins/topic_sessions/topic_messages |
| TC-78 | 测试库仅存账号/会话/密钥/记录表 | users/sessions/api_keys/records 四表 |

### 用例组14 本地核心库单测（dingao-local.js 同一份代码 node 侧执行，TC-79 ~ TC-87）
| TC-79 | 本地库加载 | 全局 DingaoLocal 可用 |
| TC-80 | 字数统计确定性 | 中文按字/英文按词，空文本=0 |
| TC-81 | 标准章节结构 | 5章（绪论…结论与展望） |
| TC-82 | 树构建与扁平化往返无损 | flatten(build(flat)) === flat |
| TC-83 | 进度统计 | 总字数=各章之和、百分位公式精确匹配 |
| TC-84 | 打卡增量与同日 upsert | 首次增量=总字数；同日再打替换且增量=新增 |
| TC-85 | 连续天数 | 今日打卡 streak≥1 |
| TC-86 | 文本导出 | 含章标题（# 第1章）与正文、子节 |
| TC-87 | 树操作 | findNode/findParent 正确 |

### 用例组15 浏览器端 Word 导出（docx IIFE 构建，TC-88 ~ TC-91）
| TC-88 | docx 导出合法 zip | PK 魔数 |
| TC-89 | 标题层级与正文 | document.xml 含 Heading1 与正文文本 |
| TC-90 | 导出确定性 | 两次构建 document.xml 100% 一致 |
| TC-91 | 大纲 docx 导出合法 | PK 魔数 |

### 用例组16 前端完整性（v0.4 本地优先，TC-92 ~ TC-97）
| TC-92 | 引入本地库与 docx 浏览器构建 | vendor/docx.iife.js + dingao-local.js |
| TC-93 | 本地库按账号分库 | IndexedDB，库名含 dingao_v04_ 与用户id |
| TC-94 | **前端不再调用服务端论文存储端点** | app.js 无 /api/chapters 等引用 |
| TC-95 | 写作自动保存为本地写库 | saveChapter 写 IndexedDB |
| TC-96 | 选题追问走纯中转 | /api/topics/iterate，无 sessions 端点 |
| TC-97 | 前端无内嵌密钥 | 三文件密钥扫描全通过 |

## 三、性能与兼容性测试（人工执行）

| 场景 | 步骤 | 通过标准 |
|---|---|---|
| 响应速度 | 首页/工作台加载；写作四视图切换 | 首屏 < 2s（内网）；无卡死 |
| 自动保存 | 编写 2000 字停顿 1 秒；刷新页面 | 内容 100% 不丢（IndexedDB 回读一致） |
| 账号隔离 | 同一浏览器换账号登录 | 各自的论文互不可见（分库隔离） |
| AI 生成 | 选题生成/大纲生成 | 首结果 < 60s（95 分位） |
| 并发 | 3 个账号同时写作+互译 | 无串号、无 500 |
| 浏览器兼容 | Chrome/Edge/Firefox/Safari 各跑核心流程 | 功能一致、无布局错乱 |
| 系统兼容 | Windows / macOS 浏览器访问 | 一致 |
| 分辨率 | 1366×768 与 4K | 工作区自动单栏（≤1100px） |
| 局域网 | 另一台电脑注册/登录/写作 | 论文数据只留在各自浏览器（F12 网络面板无论文内容上传请求） |

## 四、合规红线核对表（V1.4）

- [x] 内容0修改：校对/综述系统提示内建约束；交稿检查只检不改
- [x] AI 输出标注：选题/大纲/互译等前端固定「AI生成，需人工核验」
- [x] **论文隐私（V1.4）**：论文全文/大纲/进度/打卡仅存用户浏览器 IndexedDB（按账号分库）；服务端无论文数据表（TC-77）、无存储端点（TC-76）；AI 调用输入仅内存中转不落盘（TC-71）
- [x] 数据最小化：服务端仅存账号/会话/密钥/用户主动保存的工具记录
- [x] 密钥安全：仅服务端加载，AES-256-GCM 落库
- [x] 限流：每用户 60 次/小时 AI 请求
- [x] 引用核查/交稿检查/进度统计不走 LLM（零 AI 成本）

## 五、执行记录

- 自动化：`node test/api.test.js`（102 条，含真实 DeepSeek 调用；需先配置密钥：`DEEPSEEK_API_KEY` 环境变量或 `config/key.txt`）
- 人工：性能/兼容性矩阵由测试阶段内测用户与开发团队按上表执行
